interface Color {
    red: number;
    blue: number;
    green: number;
}

const color: Color = {
    red: 20,
    blue: 20,
    green: 22,
};

interface User {
    name: string;
}

const user: User = {
    name: "harish",
};

export default color;
